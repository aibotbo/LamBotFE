import React, { FC } from 'react';
import { Helmet } from 'react-helmet-async';

const Dashboard: FC = () => {
  return (
    <>
      <Helmet>
        <title>BotLambotrade | Dashboard</title>
      </Helmet>
      <div>Dashboard</div>
    </>
  );
};

export default Dashboard;
